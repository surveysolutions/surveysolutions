@model WB.Core.SharedKernels.SurveyManagement.Views.Interview.InterviewAttachmentViewModel

@if (Model != null)
{
    if (Model.IsImage)
    {
        <img src="@Url.RouteUrl("DefaultApiWithAction", new {httproute = "", controller = "Attachments", action = "Content", id = Model.ContentId})" width="auto" height="auto"/>
    }
}