<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WB.UI.Interviewer;assembly=WB.UI.Interviewer"
             x:Class="WB.UI.Interviewer.View.InterviewDetails"
             BindingContext="{x:Static local:ViewModelLocator.InterviewDetails}">
  <ListView x:Name="listView" ItemsSource="{Binding GroupsAndQuestions}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <ViewCell.View>
            <StackLayout Orientation="Horizontal" Padding="10,5,10,5">
              <Label Text="{Binding Title}" />
            </StackLayout>
          </ViewCell.View>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>