@model String
@{
    Layout = "~/Views/Shared/Profile/_FormFieldPartialLayout.cshtml";
}
@Html.PasswordFor(v => v, new {@class = @"form-control", autocomplete = @"off", data_val = (!ViewData.ContainsKey("disableClientValidation")).ToString().ToLower() })
