@using System.Web.Optimization
@using WB.Core.SharedKernels.SurveyManagement.Web.Models
@using WB.UI.Headquarters.Resources
@using WB.Core.SharedKernels.SurveyManagement.Web.Utils
@model LogOnModel
@{
    Layout = "~/Views/Shared/_NoAuthenticationLayout.cshtml";
    ViewBag.Title = Pages.LogOn_Title;
}
@section scripts
{
    @Scripts.Render("~/validate")
}

<div style="margin-top: 40px;">
    @Html.BootstrapValidationSummary(Pages.LogOn_UnsuccessfulLogin)
</div>

<div class="well form-signin">
    @using (Html.BeginForm("LogOn", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal" }))
    {
        <fieldset>
            <legend>
                @Pages.LogOn_Legend
            </legend>
            <div class="form-group">
                @Html.LabelFor(m => m.UserName, new { @class = "control-label col-sm-4" })
                <div class="col-sm-8">
                    @Html.TextBoxFor(m => m.UserName, new { @class = "form-control", autocomplete = "off" })
                    @Html.ValidationMessageFor(m => m.UserName, null, new { @class = "help-block" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Password, new { @class = "control-label col-sm-4" })
                <div class="col-sm-8">
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", autocomplete = "off" })
                    @Html.ValidationMessageFor(m => m.Password, null, new { @class = "help-block" })
                </div>
            </div>
        </fieldset>
        <div class="form-group">
            <div class="col-sm-offset-4 col-sm-8">
                <button class="btn btn-primary btn-lg" type="submit">@Pages.LogOn_LogIn</button>
            </div>
        </div>
    }
</div>
