@using System.Collections
@{
    ViewBag.Controller = "Questionnaire";
}

@if (Model.Count > 0)
{
    @Html.Partial("ListView")
}
else
{
    <text>@WB.UI.Designer.GlobalHelper.EmptyString</text>
}