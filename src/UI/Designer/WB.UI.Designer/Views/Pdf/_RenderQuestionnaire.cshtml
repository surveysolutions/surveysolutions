@using Main.Core.View
@model WB.UI.Designer.Views.Questionnaire.Pdf.PdfQuestionnaireView

<header>
    @this.Model.Title
</header>

@foreach (var child in this.Model.Groups)
{
    Html.RenderPartial("_RenderChild", child);
}