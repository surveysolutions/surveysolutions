<?xml version="1.0" encoding="utf-8" ?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:local="http://schemas.android.com/apk/res-auto"
      android:id="@+id/dashboard_side_menu">

    <item android:id="@+id/dashboard_your_team">
        <menu>
            <group android:checkableBehavior="single">
                <!--<item
                    android:id="@+id/dashboard_team_overview"
                    android:title="Team overview" />-->
                <item
                    android:id="@+id/dashboard_to_be_assigned"
                    android:title="Tasks to be assigned"
                    local:actionLayout="@layout/dashboard_sidebar_counter"/>
            </group>
        </menu>
    </item>
    <item android:id="@+id/dashboard_collected_interviews">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/dashboard_waiting_decision"
                    android:title="Team overview" />
                <item
                    android:id="@+id/dashboard_outbox"
                    android:title="Tasks to be assigned" />
            </group>
        </menu>
    </item>
</menu>
