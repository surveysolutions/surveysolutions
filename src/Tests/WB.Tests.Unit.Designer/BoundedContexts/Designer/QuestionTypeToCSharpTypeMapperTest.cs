using NUnit.Framework;

namespace WB.Tests.Unit.Designer.BoundedContexts.Designer
{
    /*public class QuestionTypeToCSharpTypeMapperTest
    {
        [Test]
        public void should_not_change_method_names_without_a_reason()
        {
            const string message = "Are You sure that existing assemblies are going to use it?";
            //Assert.That(nameof(IInterviewStateForExpressions.GetAnswer), Is.EqualTo("GetAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetDateTimeAnswer), Is.EqualTo("GetDateTimeAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetTextListAnswer), Is.EqualTo("GetTextListAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetGeoLocationAnswer), Is.EqualTo("GetGeoLocationAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetRosterVectorArrayAnswer), Is.EqualTo("GetRosterVectorArrayAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetRosterVectorAnswer), Is.EqualTo("GetRosterVectorAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetYesNoAnswer), Is.EqualTo("GetYesNoAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetIntArrayAnswer), Is.EqualTo("GetIntArrayAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetDoubleAnswer), Is.EqualTo("GetDoubleAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetIntAnswer), Is.EqualTo("GetIntAnswer"), message);
            //Assert.That(nameof(IInterviewStateForExpressions.GetStringAnswer), Is.EqualTo("GetStringAnswer"), message);
        }
    }*/
}
