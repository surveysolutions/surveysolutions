@model RavenQuestionnaire.Core.Views.Assignment.AssigmentBrowseItem

@{ var name = "responsible-" + Model.Id; }
 <div class="caption">
    @using (Ajax.BeginForm("Assign", "Survey", new AjaxOptions() { UpdateTargetId = @name, InsertionMode=InsertionMode.Replace}))
    {
        @Html.Hidden("Id", Model.Id)
        @Html.DropDownList("userId", ViewBag.Users as SelectList)          
        @Model.Status.Name
        
            <input name="Save" type="submit" value="Save" />
            <input name="Cancel" type="submit" value="Cancel"/>       
        
     }
     </div>
