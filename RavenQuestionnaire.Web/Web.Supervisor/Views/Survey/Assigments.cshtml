@model RavenQuestionnaire.Core.Views.Survey.SurveyBrowseView

<script src="@Url.Content("~/Scripts/jquery.unobtrusive-ajax.min.js")" type = "text/javascript"> </script>
@{
    ViewBag.Title = "Surveys";
}

<h2>Assignments</h2>
<table class="table table-striped table-bordered table-condensed">
    <thead>
        <tr>
            <th>Id</th>
            @foreach (var header in Model.Headers)
            {
                <th>@header</th>
            }
            <th>Responsible</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model.Items)
    {
        var name = "responsible-" + item.Id;
        <tr id="@name">
            @{ Html.RenderPartial("~/Views/Survey/DisplayColumn.cshtml", item); }
        </tr>
    }
</table>