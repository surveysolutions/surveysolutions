@using System.Web.Optimization
@model IEnumerable<Core.Supervisor.Views.Survey.SurveyUsersViewItem>
@{
    Layout = "~/Views/Shared/_LayoutWithSidebar.cshtml";
}

@section scripts
{
    @Scripts.Render("~/Scripts/vm/surveylistview.js")
    @Html.Partial("_ListViewStatusesScript")
    <script type="text/javascript">
        var $interviewsUrl = '@Url.Action("Interviews", "Survey")';
        $(function () {
            var model = new SurveyListViewModel('@Url.RouteUrl("DefaultApiWithAction", new { httproute = "", controller = "ReportDataApi", action = "SupervisorSurveysAndStatusesReport" })');

            ko.applyBindings(model);
            model.load();
        });
    </script>
}

@section header
{
    <h3 class="sinle-line-header">Surveys and Statuses</h3>
}

@section filters
{
    <ul>
        <li class="heading"><span>FILTER</span></li>
        <li>
            <label>Team members</label>
            <select data-bind="value: SelectedUser" class="input-block-level">
                @if (Model.Count() > 1)
                {
                    <option>Any</option>
                }
                @foreach (var item in Model)
                {
                    <option value="@item.UserId">@item.UserName</option>
                }
            </select>
        </li>
    </ul>
}

@section grid
{
    <table class="table table-striped table-bordered table-condensed table-hover">
        <thead>
            <tr>
                <th data-bind='sortby: "QuestionnaireTitle"'>Survey Name</th>
                <th data-bind='sortby: "SupervisorAssignedCount"'>Supervisor assigned</th>
                <th data-bind='sortby: "InterviewerAssignedCount"'>Interviewer assigned</th>
                <th data-bind='sortby: "SentToCapiCount"'>Sent to capi</th>
                <th data-bind='sortby: "CompletedCount"'>Completed</th>
                <th data-bind='sortby: "ApprovedBySupervisorCount"'>Approved by supervisor</th>
                <th data-bind='sortby: "RejectedBySupervisorCount"'>Rejected by supervisor</th>
                <th data-bind='sortby: "RestoredCount"'>Restored</th>
                <th data-bind='sortby: "TotalCount"'>Total</th>
            </tr>
        </thead>
        <tbody>
            <!-- ko foreach:ListView.Items -->
            <tr>
                <td>
                    <a data-bind="attr: { href: $interviewsUrl + '?templateid=' + QuestionnaireId() }, text: QuestionnaireTitle"></a>
                </td>
                <td data-bind="template: { name: 'SupervisorAssignedCountTemplate' }"></td>
                <td data-bind="template: { name: 'InterviewerAssignedCountTemplate' }"></td>
                <td data-bind="template: { name: 'SentToCapiCountTemplate' }"></td>
                <td data-bind="template: { name: 'CompletedCountTemplate' }"></td>
                <td data-bind="template: { name: 'ApprovedBySupervisorCountTemplate' }"></td>
                <td data-bind="template: { name: 'RejectedBySupervisorCountTemplate' }"></td>
                <td data-bind="template: { name: 'RestoredCountTemplate' }"></td>
                <td>
                    <a data-bind="attr: { href: $interviewsUrl + '?templateid=' + QuestionnaireId() + 'templateVersion=' + QuestionnaireVersion() }, text: TotalCount"></a>
                </td>
            </tr>
            <!-- /ko -->
        </tbody>
    </table>
}